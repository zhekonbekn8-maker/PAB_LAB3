# Введення початкових даних
d = float(input("Введіть пробіг у перший день (км): "))
t = float(input("Введіть загальну відстань (км): "))
# Ініціалізація змінних
total = 0       # сумарний пробіг
days = 0        # кількість днів
# Підрахунок днів, поки сумарний пробіг не перевищить t
while total <= t:
    total += d          # додаємо пробіг поточного дня
    d *= 1.1            # збільшуємо пробіг на 10%
    days += 1           # рахуємо день

# Вивід результату з округленням до 2-х знаків
print(f"{total:.2f} km, {days} days")
