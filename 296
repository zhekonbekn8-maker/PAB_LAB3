# Введення кількості карток
n = int(input("Введіть кількість карток: "))
# Обчислення суми всіх карток від 1 до n
total_sum = n * (n + 1) // 2
# Віднімання від повної суми тих, що залишилися
for i in range(n - 1):
    num = int(input(f"Введіть номер {i+1}-ї картки: "))
    total_sum -= num
# Виведення номера втраченої картки
print(total_sum)
