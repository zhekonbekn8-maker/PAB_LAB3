# Введення кількості днів
n = int(input("Введіть кількість днів: "))
# Ініціалізація змінної для суми помилок
total_errors = 0
# Цикл для введення кількості помилок за кожен день
for i in range(n):
    m = int(input(f"Введіть кількість помилок за {i+1}-й день: "))
    total_errors += m
# Виведення загальної кількості помилок
print(total_errors)
